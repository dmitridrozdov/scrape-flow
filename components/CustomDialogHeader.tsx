"use client"

import { DialogHeader, DialogTitle } from "@/components/ui/dialog"

import React from 'react'

interface Props {
    title?: string
    subTitle?: string
    icon?: string
    iconClassName?: string
}
const CustomDialogHeader = (props: Props) => {
  return (
    <div>CustomDialogHeader</div>
  )
}

export default CustomDialogHeader